---
import BaseHead from '@/components/layout/BaseHead.astro'
import Footer from '@/components/layout/BaseFooter.astro'
import Header from '@/components/layout/Header.astro'
import { ViewTransitions } from "astro:transitions"
import { languages } from '@/lib/utils'

const { language, langSwitch } = Astro.props
const { title } = Astro.props

console.log('-------------------');
console.log(language, langSwitch);
console.log('-------------------');


const { showHeader = true, showFooter = true } = Astro.props
const mainClass = showHeader && "pt-20"
---
<html lang={Astro.currentLocale}>
  <head>
    <BaseHead description="Test description" title={title} transition:persist/>
    <ViewTransitions />
  </head>
  <body>
    {showHeader && <Header />}
    <main class={mainClass}>
        <slot />
    </main>
    {showFooter && <Footer />}
  </body>
</html>